# GEMINI_API_KEY: Required for Gemini AI strategic operations and executive triage.
# AI Studio automatically injects this at runtime from user secrets.
GEMINI_API_KEY=

# APP_URL: The URL where this applet is hosted.
APP_URL=

# ==========================================
# SUPABASE MULTI-TENANT SAAS CONFIGURATION
# ==========================================
# 1. Provide your Supabase project URL and anon public API key:
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-public-key"

# 2. Complete Multi-Tenant Database Schema (Run in Supabase SQL Editor):
#
# -- 1. COMPANIES TABLE (Tenant Workspaces)
# CREATE TABLE IF NOT EXISTS public.companies (
#   id TEXT PRIMARY KEY,
#   name TEXT NOT NULL,
#   owner_id TEXT NOT NULL,
#   industry TEXT DEFAULT 'Enterprise SaaS',
#   target_arr NUMERIC DEFAULT 10000000,
#   plan TEXT DEFAULT 'Pro', -- 'Starter' | 'Pro' | 'Enterprise' | 'Trial'
#   mrr NUMERIC DEFAULT 999,
#   status TEXT DEFAULT 'active', -- 'active' | 'trialing' | 'expired' | 'canceled'
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
#   updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
# );
#
# -- 2. USERS TABLE (Linked to company_id)
# CREATE TABLE IF NOT EXISTS public.users (
#   id TEXT PRIMARY KEY,
#   email TEXT NOT NULL,
#   full_name TEXT,
#   company_id TEXT REFERENCES public.companies(id),
#   role TEXT DEFAULT 'CEO',
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
# );
#
# -- 3. SUBSCRIPTIONS TABLE (Multi-Tenant Billing)
# CREATE TABLE IF NOT EXISTS public.subscriptions (
#   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
#   company_id TEXT REFERENCES public.companies(id),
#   user_id TEXT NOT NULL,
#   status TEXT NOT NULL DEFAULT 'trialing', -- 'trialing' | 'active' | 'expired' | 'canceled'
#   trial_ends_at TIMESTAMP WITH TIME ZONE,
#   ai_actions_remaining INT DEFAULT 50,
#   plan TEXT DEFAULT 'Pro', -- 'Starter' | 'Pro' | 'Enterprise'
#   plan_price NUMERIC DEFAULT 999,
#   stripe_customer_id TEXT,
#   stripe_subscription_id TEXT,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
#   updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
#   UNIQUE(company_id)
# );
#
# -- 4. DEALS TABLE (Multi-Tenant Revenue Radar & CRM)
# CREATE TABLE IF NOT EXISTS public.deals (
#   id TEXT PRIMARY KEY,
#   company_id TEXT REFERENCES public.companies(id) NOT NULL,
#   user_id TEXT,
#   account_name TEXT NOT NULL,
#   contact_name TEXT,
#   contact_email TEXT,
#   contact_role TEXT,
#   deal_value NUMERIC DEFAULT 0,
#   stage TEXT DEFAULT 'Discovery',
#   type TEXT DEFAULT 'LEAD', -- 'CLIENT' | 'LEAD'
#   health_score INT DEFAULT 85,
#   risk_factors JSONB DEFAULT '[]'::jsonb,
#   notes TEXT,
#   last_contact TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
#   ai_action JSONB,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
#   updated_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
# );
#
# -- 5. INBOX TABLE (Multi-Tenant Autonomous Email Triage)
# CREATE TABLE IF NOT EXISTS public.inbox (
#   id TEXT PRIMARY KEY,
#   company_id TEXT REFERENCES public.companies(id) NOT NULL,
#   user_id TEXT,
#   sender TEXT NOT NULL,
#   sender_email TEXT NOT NULL,
#   subject TEXT NOT NULL,
#   snippet TEXT,
#   full_body TEXT,
#   urgency TEXT DEFAULT 'MEDIUM', -- 'HIGH' | 'MEDIUM' | 'LOW'
#   category TEXT DEFAULT 'CLIENT',
#   status TEXT DEFAULT 'PENDING_REVIEW', -- 'PENDING_REVIEW' | 'APPROVED' | 'IGNORED' | 'SENT'
#   received_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()),
#   ai_draft_reply TEXT,
#   ai_key_takeaway TEXT,
#   ai_suggested_action TEXT,
#   approved_at TIMESTAMP WITH TIME ZONE
# );
#
# -- 6. BRIEFINGS TABLE (Multi-Tenant 9:00 AM Executive Briefings)
# CREATE TABLE IF NOT EXISTS public.briefings (
#   id TEXT PRIMARY KEY,
#   company_id TEXT REFERENCES public.companies(id) NOT NULL,
#   user_id TEXT,
#   briefing_date TEXT NOT NULL,
#   headline TEXT,
#   arr_forecast NUMERIC,
#   churn_risk_arr NUMERIC,
#   top_priorities JSONB,
#   action_items JSONB,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now())
# );
#
# -- Enable Row Level Security (RLS) policies based on company_id:
# ALTER TABLE public.companies ENABLE ROW LEVEL SECURITY;
# ALTER TABLE public.deals ENABLE ROW LEVEL SECURITY;
# ALTER TABLE public.inbox ENABLE ROW LEVEL SECURITY;
# ALTER TABLE public.briefings ENABLE ROW LEVEL SECURITY;
# ALTER TABLE public.subscriptions ENABLE ROW LEVEL SECURITY;

